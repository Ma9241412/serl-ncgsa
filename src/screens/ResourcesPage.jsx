import React from 'react';
import ResourceMain from '../MainComponents/ResourceComponents/ResourceMain';

const ResourcesPage = () => {
  return <ResourceMain />;
};

export default React.memo(ResourcesPage);
